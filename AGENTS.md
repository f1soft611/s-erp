# AGENTS

## 프로젝트 개요

이 워크스페이스는 분리형 ERP 애플리케이션입니다.

- 프론트엔드: React + TypeScript + Vite, 위치 [frontend](frontend)
- 백엔드: Java + Spring Boot / eGovFrame, 위치 [backend](backend)
- 요구사항, 계획, 사양, 작업 결과 문서: [docs](docs)

AI 에이전트는 코드 수정 전에 [docs](docs) 를 우선 확인해야 합니다. 문서는 기능 요구사항의 최종 근거이며, 구현은 문서와 작업지시서를 기준으로 수행해야 합니다. 임의로 기능을 추가하거나 화면 동작을 추측해서 구현하면 안 됩니다.

## 개발 원칙

### 1. 문서 우선

- 구현 전, [docs/spec](docs/spec), [docs/plan](docs/plan), [docs/directions](docs/directions) 를 먼저 확인합니다.
- 문서에 명시된 흐름, 필드명, 권한 정책, 화면 동작을 우선 반영합니다.
- 요구사항이 불명확하면 추측 대신 문서를 추가 확인하거나 필요한 정보를 요청합니다.
- 제품 기능을 추가할 때는 기존 화면/기능과 충돌하지 않는지 먼저 검토합니다.

### 1.1 날짜별 문서 관리

- 작업지시서, 계획서, 사양서는 각각 `docs/directions/YYYYMMDD`, `docs/plan/YYYYMMDD`, `docs/spec/YYYYMMDD`에 작성합니다.
- 결과 문서와 스크린샷은 `docs/result/YYYYMMDD/<work-slug>`에 함께 보관합니다.
- 문서 파일명은 기존 `YYYYMMDD_NNN_제목_문서유형.md` 규칙을 유지하고, 날짜가 없는 샘플 문서는 해당 문서 종류의 루트에 둡니다.
- 문서 간 상대 링크는 날짜 하위 폴더 위치를 기준으로 작성하고, 문서 이동 시 함께 갱신합니다.

### 2. 변경 범위 최소화

- 프론트엔드와 백엔드 수정은 해당 폴더에만 국한합니다.
- 기존 구조를 벗어난 대규모 리팩터링을 하지 않습니다.
- 한 작업에서 여러 기능을 한꺼번에 수정하지 않고, 하나의 요구사항 단위로 나눠 진행합니다.
- 기존 네이밍 규칙, 디렉터리 구조, 파일 역할을 우선 따릅니다.

### 3. 책임 분리 유지

- 프론트엔드: 화면, 라우팅, 컴포넌트, 서비스, 타입, 테마를 각각 적절한 위치에서 관리합니다.
- 백엔드: Controller, Service, Mapper/DAO, Config, Resource 계층을 명확히 유지합니다.
- 비즈니스 로직을 UI와 섞지 않고, 서버와 클라이언트의 책임을 분리합니다.
- 공통 로직은 기존 패턴을 따라 재사용 가능한 형태로 정리합니다.

### 4. 검증 우선

- 기능 수정 전에 가능한 경우 실패하는 테스트를 추가하거나 관련 테스트를 먼저 실행합니다.
- 수정 후에는 최소 검증 범위로 바로 확인합니다.
- 스모크 테스트가 아니라 실제 동작에 가까운 기준으로 검증합니다.

## 작업 시작 전 체크리스트

1. 관련 문서 확인: [docs](docs) 에서 요구사항 또는 작업지시서를 찾음
2. 작업지시서 확인: 작업 절차와 최종 완료 조건을 먼저 점검
3. 계획서 작성: [docs/plan](docs/plan) 에서 요구사항을 구현 단위로 정리
4. 상세 사양서 작성: [docs/spec](docs/spec) 에서 화면/필드/API/검증 기준을 정의
5. 영향 범위 확인: 프론트/백엔드 어느 쪽이 필요한지 판단
6. 기존 패턴 확인: 동일 기능이 이미 구현되어 있는지 확인
7. 최소 수정 계획 수립: 파일과 함수 단위로 범위를 좁힘
8. 검증 계획 수립: 해당 기능에 맞는 build/test 실행 준비
9. DB 변경 점검: 백엔드 수정이 있으면 신규 테이블/컬럼 변경 여부를 먼저 확인

### 필수 실행 순서

에이전트는 아래 순서를 절대 우회해서는 안 됩니다.

1. 작업지시서 확인
2. 작업지시서를 기반으로 계획서 작성 (`/docs/plan/YYYYMMDD`)
3. 계획서를 기반으로 상세 사양서 작성 (`/docs/spec/YYYYMMDD`)
4. 사양서에 따라 실제 구현
5. 결과물 작성 및 스크린샷 포함 (`/docs/result/YYYYMMDD/<work-slug>`)
6. 백엔드 DB 변경이 있으면 스크립트와 스키마 관리 여부를 점검

---

## 전용 문서 연결 구조

이 프로젝트는 루트 규칙과 영역별 전용 규칙을 분리해서 관리합니다.

- 전체 프로젝트 기준: [AGENTS.md](AGENTS.md)
- 프론트엔드 전용 가이드: [frontend/AGENTS.md](frontend/AGENTS.md)
- 백엔드 전용 가이드: [backend/AGENTS.md](backend/AGENTS.md)

AI 에이전트는 작업 유형에 따라 아래 우선순위를 따라야 합니다.

1. 루트 [AGENTS.md](AGENTS.md) 에서 전체 원칙 확인
2. 작업 영역에 맞는 전용 문서 확인
3. 해당 영역의 구현 및 검증 기준 적용
4. 문서와 코드가 일치하는지 최종 점검

## 프론트엔드 규칙

- 소스 코드는 [frontend/src](frontend/src) 에서 관리합니다.
- React 컴포넌트는 기존 구조와 컴포넌트 분리 패턴을 따라야 하며, 필요 시 재사용 가능한 레이어로 정리합니다.
- API 호출은 서비스 계층을 통해 수행하고, 화면 컴포넌트는 비즈니스 로직을 직접 과도하게 담지 않습니다.
- 타입 정의는 기존 구조에 맞춰 정리하고, 임의로 광범위한 타입 변환을 만들지 않습니다.
- 화면 변경 시 라우팅, 상태 관리, 에러 처리, 로딩 상태까지 함께 확인합니다.

### 프론트엔드 검증 명령

프로젝트 스크립트는 [frontend/package.json](frontend/package.json) 에 정의되어 있습니다.

- `npm run dev`: Vite 개발 서버 실행, 기본 주소는 `127.0.0.1:4173`
- `npm run build`: TypeScript + Vite 빌드
- `npm run test`: jsdom 환경에서 Vitest 단일 실행
- 특정 테스트 파일 실행: `npm run test -- tests/f1-grid.test.tsx`

---

## 백엔드 규칙

- 서버 코드는 [backend/src/main/java](backend/src/main/java) 에 작성합니다.
- Spring Boot / eGovFrame 구조를 유지하고, 레이어별 책임을 명확히 나눠야 합니다.
- 설정값과 보안 관련 민감 정보는 하드코딩하지 않고, 환경변수 또는 설정 파일을 통한 주입을 우선합니다.
- 새로운 API는 기존 컨트롤러/서비스/매퍼 패턴과 일관되게 구성합니다.
- 응답 구조, 파라미터명, 상태 코드, 보안 정책은 기존 API 스타일과 맞춰야 합니다.
- 백엔드 작업에서 신규 테이블 생성, 컬럼 추가/수정/삭제, FK 변경, 기본값 변경, 데이터 마이그레이션이 필요한 경우 반드시 SQL 스크립트를 생성합니다.
- DB 변경 작업은 코드 수정만으로 끝내지 않고, 스키마 반영 내역을 관리 가능한 스크립트 형태로 남겨야 합니다.
- 스키마 관리 방식은 단일 파일 갱신이 아니라 날짜별 스키마 폴더를 운영하고, 각 변경 이력을 별도 파일로 기록해서 추적 가능하게 유지합니다.
- 최종적으로는 `docs/database` 또는 `backend/DATABASE` 와 같은 스키마 디렉터리 아래에 날짜 기반 변경본을 누적 관리하고, 변경 시점/내용/영향 범위, 롤백 여부를 함께 기록해야 합니다.
- 변경 이력 파일에는 날짜, 변경 내용, 적용 대상 테이블, 영향 범위, 롤백 여부를 포함해 추적 가능하게 정리합니다.

### 백엔드 검증 명령

```bash
cd backend
mvn test
mvn spring-boot:run
```

백엔드 구조와 실행 정보는 [backend/README.md](backend/README.md) 를 참고합니다. 최소 Java 17 이상, Maven 3.8.x 환경을 기준으로 동작합니다.

### DB 스키마 폴더 구조 예시

```text
docs/
  database/
    db-schema.md                # 전체 테이블 누적 스키마 문서(단일 파일)
    20260831/
      20260831_001_create_login_schema.sql
      20260831_001_change_login_schema.md
      20260831_001_rollback.sql
      20260831_002_seed_admin_tenant.sql
      20260831_003_create_module_menu_schema.sql
      20260831_003_change_module_menu_schema.md
      20260831_003_rollback.sql
    20260901/
      20260901_001_add_menu_description.sql
      20260901_001_change_menu_description_schema.md
      20260901_001_rollback.sql

backend/
  DATABASE/
    20260831/
      20260831_001_create_login_schema.sql
      20260831_001_change_login_schema.md
      20260831_001_rollback.sql
    20260901/
      20260901_001_add_menu_description.sql
      20260901_001_change_menu_description_schema.md
      20260901_001_rollback.sql
```

규칙:

- 날짜 폴더명은 `YYYYMMDD` 형식(하이픈 없이)으로 통일합니다.
- `docs/database`와 `backend/DATABASE`에 동일한 날짜 폴더와 파일을 동일하게 유지합니다.
- 같은 작업 번호(`NNN`)로 SQL, 변경 이력 문서(`.md`), 롤백 스크립트를 함께 남기고, seed 등 추가 스크립트가 있으면 같은 `NNN`을 재사용합니다.

- 날짜별 폴더를 만들고 수정 내역을 별도 파일로 남깁니다.
- SQL 스크립트와 변경 이력 문서를 함께 보관합니다.
- 신규 테이블, 컬럼 추가/삭제, FK 변경, 데이터 마이그레이션은 반드시 이 폴더에 기록합니다.
- 최종적으로는 날짜별 스키마 이력을 기준으로 현재 상태를 추적할 수 있어야 합니다.

---

## 문서 우선 원칙

- 구현 전, 사양 문서와 작업지시서를 항상 확인합니다.
- 문서는 기능 결정의 출처이며, 코드보다 우선합니다.
- 작업 중에 문서와 실제 코드가 어긋나면 먼저 문서가 맞는지 확인하고, 코드가 의도와 다르면 정합성을 맞춥니다.
- 문서가 없는 경우에는 기존 코드 패턴을 참고하되, 요구사항을 임의로 추가하지 않습니다.
- 구현 단계에 들어가기 전에 [docs/plan](docs/plan) 와 [docs/spec](docs/spec) 에서 정리된 내용을 우선 반영합니다.
- 작업지시서의 순서와 완료 기준을 무시하고 바로 코딩하는 행위를 금지합니다.
- F1-Grid 관련 기능 수정/변경/버그 수정/추가 작업은 반드시 문서 갱신을 같은 작업으로 포함해야 합니다.
  - 문서와 코드가 같이 변경되지 않으면 작업을 완료로 간주하지 않습니다.
  - 관련 문서 예시: [frontend/src/pages/f1-grid-docs/F1-GRID.md](frontend/src/pages/f1-grid-docs/F1-GRID.md), [docs/superpowers](docs/superpowers), [docs/result](docs/result)
  - 변경 항목이 UI 동작, 컬럼/행 동작, 편집 모드, 이벤트, 옵션, 검증 기준, 기대 동작 중 하나라도 포함되면 문서도 동일 범위에서 반영해야 합니다.
- F1-Grid 문서 갱신 기준:
  - 화면 동작 변경 시 사용자 흐름/동작 설명 업데이트
  - API 또는 데이터 구조 변경 시 예시값, 필드 설명, 검증 기준 업데이트
  - 버그 수정 시 재현 조건, 원인, 해결 방법, 회귀 검증 항목을 결과 문서에 추가
  - 기능 추가/삭제 시 해당 문서와 결과 문서 모두 최신 상태로 유지

## 안전한 작업 습관

- 수정 범위를 넘어가는 파일 검색이나 무관한 리팩터링을 하지 않습니다.
- 생성된 결과물, 빌드 산출물, target 폴더를 임의로 수정하지 않습니다.
- 현재 작업의 목적과 다른 변경을 함께 넣지 않습니다.
- 에러 메시지를 확인하고 원인에 기반해서 수정합니다. 단순 시도와 반복적 수정은 피합니다.
- 스크린샷, 문서 결과물, 로그 파일 저장 경로는 한글 폴더를 사용하지 않습니다. 파일 시스템/PowerShell/Playwright 경로 인식 오류를 피하기 위해 영문 소문자, 숫자, 하이픈만 사용합니다.
  - 예시: `docs/result/20260827/erp-login-dashboard/screenshots`
  - 금지: `docs/result/ERP-로그인-API-연동-대시보드/screenshots`

## 팀 협업 규칙

- 프론트와 백엔드가 서로를 의존하게 만드는 변경은 최소화합니다.
- 알 수 없는 계약(필드명, 응답 형태, API 경로, 상태 값)을 임의로 만들지 않습니다.
- 기존 API와 화면에서 이미 쓰는 키/필드명을 우선 존중합니다.
- 변경 전후에 영향 범위를 검토하고, 변경사항이 필요한 위치를 함께 점검합니다.

## 유용한 참고 경로

- [.github/f1workflow/README.md](.github/f1workflow/README.md) - Superpowers 기반 F1Workflow 지능형 워크플로우 가이드 (`/f1workflow`)
- [backend/README.md](backend/README.md)
- [frontend/README.md](frontend/README.md)
- [frontend/package.json](frontend/package.json)
- [docs](docs)
- [docs/spec](docs/spec)
- [docs/plan](docs/plan)
- [docs/directions](docs/directions)

## 작업 종료 기준

작업을 마무리하기 전에는 다음을 확인합니다.

1. 요구사항 문서와 구현 결과가 일치하는가
2. 수정 범위가 최소 범위로 유지되었는가
3. 프론트엔드/백엔드 각각의 검증이 수행되었는가
4. 기존 패턴과 네이밍 규칙을 위반하지 않았는가
5. 변경으로 인한 영향 범위가 정상적으로 검토되었는가
6. 결과 문서와 스크린샷이 [docs/result](docs/result) 에 포함되었는가
7. 백엔드 DB 변경이 있었다면 SQL 스크립트와 스키마 이력 갱신이 완료되었는가

## 필수 체크리스트 최종판

- [ ] 작업지시서 확인
- [ ] 작업지시서를 기반으로 계획서 작성 (/docs/plan)
- [ ] 계획서를 기반으로 상세 사양서 작성 (/docs/spec)
- [ ] 사양서에 따라 실제 구현
- [ ] 결과물 작성 및 스크린샷 포함 (/docs/result)
- [ ] 백엔드 작업 시 신규 테이블/컬럼/변경 사항에 대한 SQL 스크립트 생성
- [ ] 날짜별 스키마 폴더에 변경 이력을 누적하고 관리했는가
- [ ] 변경 이력 파일에 날짜/내용/영향 범위가 기록되었는가
- [ ] 최종 완료 전 필수 체크리스트 재검증

이 문서는 AI 에이전트가 이 프로젝트의 개발 규칙, 검증 방식, 문서 우선 원칙, 그리고 DB 변경 관리 기준을 빠르게 이해하도록 돕는 기준 문서입니다.

