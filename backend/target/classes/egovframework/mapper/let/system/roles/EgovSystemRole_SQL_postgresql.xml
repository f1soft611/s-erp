<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="SystemRoleDAO">

<resultMap id="role" type="egovframework.let.system.roles.domain.model.SystemRoleVO">
<result property="roleId" column="role_id"/>
<result property="tenantId" column="tenant_id"/>
<result property="roleCode" column="role_code"/>
<result property="roleNm" column="role_nm"/>
<result property="roleDc" column="role_dc"/>
<result property="useAt" column="use_at"/>
<result property="isSystemRole" column="is_system_role"/>
<result property="createdAt" column="created_at"/>
<result property="updatedAt" column="updated_at"/>
</resultMap>

<select id="selectRoleList" resultMap="role">
SELECT role_id, tenant_id, role_code, role_nm, role_dc, use_at, is_system_role, created_at, updated_at
FROM tb_role
WHERE tenant_id = #{tenantId}
<if test="useAt != null and useAt != ''">
AND use_at = #{useAt}
</if>
ORDER BY role_id ASC
</select>

<select id="selectRoleById" parameterType="map" resultMap="role">
SELECT role_id, tenant_id, role_code, role_nm, role_dc, use_at, is_system_role, created_at, updated_at
FROM tb_role
WHERE role_id = #{roleId} AND tenant_id = #{tenantId}
</select>

<select id="selectRoleIdByCode" parameterType="map" resultType="long">
SELECT role_id FROM tb_role
WHERE tenant_id = #{tenantId} AND role_code = #{roleCode}
</select>

<select id="insertRole" parameterType="map" resultType="long">
INSERT INTO tb_role (tenant_id, role_code, role_nm, role_dc, is_system_role, use_at)
VALUES (#{tenantId}, #{roleCode}, #{roleNm}, #{roleDc}, 'N', #{useAt})
RETURNING role_id
</select>

<update id="updateRole" parameterType="map">
UPDATE tb_role
SET role_nm = #{roleNm},
    role_dc = #{roleDc},
    use_at = #{useAt},
    updated_at = now()
WHERE role_id = #{roleId} AND tenant_id = #{tenantId}
</update>

</mapper>
