# 상세 사양서

## 화면 동작

- 활성 셀 또는 편집 중인 셀은 2px solid primary 테두리를 표시한다.
- 드래그 선택 범위는 단일 외곽선 레이어만 유지하며 각 셀 border를 추가하지 않는다.
- 테두리 두께는 2px를 유지하고 좌우/상하 불균형을 만들지 않는다.

## 검증 기준

- `npm run test -- tests/f1-grid.test.tsx` 통과
- 선택 영역과 활성 셀 상태가 명확히 구분되어 보이는지 확인
