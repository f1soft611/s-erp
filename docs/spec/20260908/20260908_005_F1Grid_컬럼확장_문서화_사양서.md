# 상세 사양서

## 관련 문서

- 작업지시서: [20260908*005_F1Grid*컬럼확장*문서화*작업지시서.md](../../directions/20260908/20260908_005_F1Grid_컬럼확장_문서화_작업지시서.md)
- 계획서: [20260908*005_F1Grid*컬럼확장*문서화*계획서.md](../../plan/20260908/20260908_005_F1Grid_컬럼확장_문서화_계획서.md)

## 변경 대상 파일

- `frontend/src/pages/f1-grid-docs/F1-GRID.md` (문서만 변경, 코드 변경 없음)

## 1. `# 5. Grid Column Specification` 섹션에 추가할 내용

기존 `F1GridColumn` 인터페이스 설명 블록과 "Row Merge 설정" 하위 섹션 사이(또는 인터페이스 블록 직후)에 아래 하위 섹션을 신규 삽입한다.

### 삽입 위치

- `interface F1GridColumn<T> { ... }` 코드 블록과 그 아래 `> ⚠️ 아직 미구현: ...` 안내문 다음, `Row Merge 설정:` 문단 이전.

### 삽입할 마크다운 (그대로 반영)

````markdown
## 컬럼 확장 API (표시 커스터마이징)

컬럼별로 표시 영역, 스타일, DOM 속성을 개발자가 직접 확장할 수 있다.

```typescript
type F1GridCellRenderContext<T> = {
  row: T;
  rowId: string | number;
  column: F1GridColumn<T>;
  field: keyof T;
  value: unknown;
  rowIndex: number;
};

type F1GridCellProps = {
  className?: string;
  style?: React.CSSProperties;
  title?: string;
  'aria-label'?: string;
};

interface F1GridColumn<T> {
  renderCell?: (context: F1GridCellRenderContext<T>) => React.ReactNode;
  getCellStyle?: (
    context: F1GridCellRenderContext<T>,
  ) => React.CSSProperties | undefined;
  getCellProps?: (
    context: F1GridCellRenderContext<T>,
  ) => F1GridCellProps | undefined;
}
```
````

- `renderCell`은 **표시 모드에서만** 적용된다. 셀이 편집 모드로 전환되면 `renderCell`은 무시되고 기존 `CellEditor`/editor plugin 흐름이 그대로 사용된다.
- `getCellStyle`과 `getCellProps.style`은 문자열 CSS가 아닌 `React.CSSProperties` 객체를 반환해야 한다. 두 값이 모두 있으면 병합되어 적용된다.
- `getCellProps`가 반환하는 `className`, `title`, `aria-label`은 셀 컨테이너(`role="gridcell"`)에 그대로 반영된다.
- 세 함수 모두 미지정 시 기존 값 표시/편집 동작이 그대로 유지된다(하위 호환).
- 반복되거나 복잡한 스타일은 `getCellProps.className`과 외부 CSS 클래스로 확장하는 것을 권장한다.

사용 예시:

```typescript
const columns: F1GridColumn<Item>[] = [
  {
    field: 'status',
    headerName: '상태',
    renderCell: ({ value }) => (
      <span className={value === 'ACTIVE' ? 'badge-active' : 'badge-inactive'}>
        {value === 'ACTIVE' ? '사용' : '미사용'}
      </span>
    ),
    getCellStyle: ({ value }) =>
      value === 'ACTIVE' ? { fontWeight: 600 } : undefined,
    getCellProps: ({ row }) => ({
      title: row.statusUpdatedAt ? `변경일: ${row.statusUpdatedAt}` : undefined,
    }),
  },
];
```

````

## 2. `# 26. Status / UI` 섹션에 추가할 내용

### 삽입 위치

- `# 26. Status / UI` 섹션 본문 마지막(다음 섹션 `# 27. 접근성` 시작 전).

### 삽입할 마크다운 (그대로 반영)

```markdown
## 편집 가능 컬럼 헤더 표시

편집 가능한 컬럼은 헤더 하단에 강조 색상(`primary.main`) border로 표시된다.

판정 조건(모두 만족해야 표시):

1. 그리드에 활성화된 editor plugin이 하나 이상 존재한다.
2. 화면에 보이는 행(`visibleRows`) 중 하나 이상에서 `isCellEditable(column, row)`이 참이다. 즉 컬럼의 `editable: true` 선언만으로는 표시되지 않으며, 행별 `editable` 함수 결과와 실제 데이터를 함께 판단한다.
3. 활성 editor plugin에 `canEdit(context)`가 정의되어 있다면 그 조건도 함께 만족해야 한다(모든 plugin의 `canEdit`을 통과해야 함).

구현 참고:

- 헤더 셀에는 `data-editable-column="true"` 속성이 추가되어 테스트/디버깅에서 식별할 수 있다.
- 헤더 하단 border 표시는 컬럼 정렬 인디케이터, 필터 적용 표시, 컬럼 고정(pinned), 드래그앤드롭 드롭 위치 표시(`data-drop-target`)와 동일한 헤더 셀에 공존하며 서로 가리지 않는다.
````

## 3. 문서-코드 정합성 검증 기준

- 위 삽입 내용 중 타입/조건 설명은 아래 코드 근거와 정확히 일치해야 한다.
  - `frontend/src/shared/components/f1-grid/types/grid.types.ts` (`F1GridCellRenderContext`, `F1GridCellProps`, `F1GridColumn.renderCell/getCellStyle/getCellProps`)
  - `frontend/src/shared/components/f1-grid/core/GridCell.tsx` (표시 모드 분기, `getCellProps`/`getCellStyle` 병합)
  - `frontend/src/shared/components/f1-grid/core/F1Grid.tsx` (`editableColumnFields` 계산 로직)
  - `frontend/src/shared/components/f1-grid/core/GridHeader.tsx` (`isEditableHeader`, `data-editable-column`, border 스타일)
- 구현 과정에서 코드와 문서 초안이 다르면 코드를 기준으로 문서를 수정한다(코드가 최종 진실 소스).

## 검증 조건 (사양 기준)

1. 삽입된 두 하위 섹션이 `F1-GRID.md`에 존재하는지 확인.
2. 코드 변경 파일이 없는지(`git diff --stat`에 `F1-GRID.md`만 표시) 확인.
3. `npm run test -- tests/f1-grid.test.tsx` 실행 결과가 기존과 동일(회귀 없음)한지 확인.
4. 결과 문서에 변경 요약과 테스트 로그를 남긴다.
