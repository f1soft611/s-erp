# 20260828_002 F1-GRID 폴더 구조 및 명칭 정리 작업지시서

## 1. 목적

docs/guide/F1-GRID.md 사양에 맞게 `frontend/src/shared/components/f1-grid`의 폴더 구조를 재정리하고, 코드 내에 남아 있는 `ERPGrid` 명칭을 `F1Grid`로 통일 및 정리한다.

## 2. 작업 범위

- `core/ERPGrid.tsx` -> `core/F1Grid.tsx` 로 파일 및 컴포넌트 명칭 변경
- `f1Grid.state.ts` -> `state/GridState.ts` (또는 `state/grid.state.ts`) 로 이동 및 명칭 정리
- `f1Grid.merge.ts` -> `merge/GridRowMerge.ts` 로 이동 및 명칭 정리
- `f1Grid.types.ts` & `types/grid.types.ts` 통합 및 정리 (`types/grid.types.ts`)
- `f1Grid.utils.ts` & `utils/grid.utils.ts` 통합 및 정리 (`utils/grid.utils.ts`)
- `index.ts` 메인 엔트리 추가 및 `F1Grid.tsx` 내 re-export 하위 호환성 정리
- 기존 import 참조 경로 수정 (`MenuManagementPanel.tsx`, `f1-grid.test.tsx` 등)

## 3. 완료 조건

- `ERPGrid`라는 컴포넌트/파일 명칭 잔재가 완전히 제거되고 `F1Grid`로 통일됨
- F1-GRID.md 구조에 부합하도록 state, merge, types, utils 모듈 폴더화 및 정리 완료
- `npm run test` 단위/통합 테스트 모두 통과
- `npm run build` 빌드 성공
- 결과 문서 작성 (`docs/result/20260828/f1-grid-structure-cleanup/`)
