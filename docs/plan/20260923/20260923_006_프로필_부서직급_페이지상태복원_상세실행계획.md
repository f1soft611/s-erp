# 프로필 부서·직급 및 페이지 상태 복원 상세 실행 계획

## Global Constraints

- 현재 브랜치 `socra710`에서 진행하고 기존 사용자 변경을 되돌리지 않는다.
- 계획서·상세 사양서의 범위만 구현한다.
- DB 변경은 이미 작성된 20260923_006 SQL·롤백 계약과 일치시킨다.
- Java 8 호환 문법과 기존 MyBatis/eGovFrame 패턴을 유지한다.
- F1-Grid 렌더링·편집 엔진은 수정하지 않고 선택 행 복원용 최소 ref API만 확장한다. 정렬·필터는 저장하지 않는다.
- 공통코드 `Splitter`의 좌측 분할 크기를 페이지 상태에 저장·복원한다.
- 각 태스크는 구현 후 좁은 검증을 실행하고 작업 원장에 결과를 기록한다.

## Interfaces

- Backend profile response fields: `departmentName`, `levelId`, `levelCode`, `levelName`.
- Frontend page-session key format: `s-erp:page:<pageKey>`.
- Common-code page key: `common-code-management`.
- Existing recent-menu key remains `s-erp:recent-menu-history`.

### Step 1: DB migration contract verification

- Files: `backend/DATABASE/20260923/20260923_006_프로필_직급_FK_추가.sql`, `docs/database/20260923/20260923_006_프로필_직급_FK_추가.sql`, rollback/change-schema files, `docs/database/db-schema.md`.
- Work: compare both SQL copies, FK target, `ON DELETE RESTRICT`, nullable behavior, and cumulative schema wording.
- Validation: SQL text comparison and `git diff --check` for documentation files.
- Commit checkpoint: user chooses integration later; no automatic commit in current dirty branch.

### Step 2: Backend profile model and mapper

- Files: `backend/src/main/java/egovframework/com/cmm/LoginVO.java`, `backend/src/main/resources/egovframework/mapper/let/uat/uia/EgovLoginUsr_SQL_postgresql.xml`, relevant login/menu VOs and service files.
- Work: add nullable level ID/code/name and department name fields; join `tb_department` and active tenant-scoped `LEVEL` common-code item; preserve old fields and fallback nulls.
- Validation: focused backend compile and mapper/login tests.
- Interface output: authenticated `LoginVO` contains profile fields for JWT/menu response.

### Step 3: JWT and my-menu response

- Files: `backend/src/main/java/egovframework/com/jwt/EgovJwtTokenUtil.java`, `backend/src/main/java/egovframework/let/system/menus/domain/model/MyMenuResponseVO.java`, `MyMenuApiController.java`, related tests.
- Work: carry only display fields in JWT, restore them defensively for old tokens, and expose profile fields in `/api/v1/menus/my`.
- Validation: JWT round-trip test and my-menu response/controller test.
- Interface output: frontend menu response has department and level fields.

### Step 4: Profile popover UI

- Files: `frontend/src/pages/dashboard/types/dashboard.ts`, `frontend/src/pages/dashboard/DashboardPage.tsx`, dashboard tests.
- Work: render department and level as primary profile metadata, retain role as secondary fallback, handle missing fields without blocking menu loading.
- Validation: focused Vitest profile test and frontend build.
- Interface output: profile popover consumes the expanded user summary.

### Step 5: Generic page-session hook contract

- Files: `frontend/src/shared/hooks/usePageSessionState.ts`, hook tests.
- Work: preserve existing API compatibility while supporting version/validation where needed; keep reset from rewriting initial state after logout; isolate keys by page.
- Validation: restore, persist, invalid JSON, version mismatch, reset tests.
- Interface output: page components can persist serializable view state safely.

### Step 6: Common-code page restoration

- Files: `frontend/src/pages/co/master/common-code/CommonCodeManagementPage.tsx`, `CommonCodeManagementPanel.tsx`, types/tests.
- Work: persist search query, selected group ID/row IDs, and valid detail selection IDs; restore after group data loads; fall back to first valid group; never restore dirty edits.
- Validation: component tests for search/group/detail restoration and invalid group fallback.
- Interface output: common-code state survives route navigation in the same login session.

### Step 7: Logout cleanup

- Files: `frontend/src/shared/hooks/usePageSessionState.ts`, `DashboardPage.tsx`, auth/session tests.
- Work: ensure the known page keys and recent-menu key are cleared on logout without affecting theme or unrelated local storage.
- Validation: logout reset test and recent-menu regression test.

### Step 8: Full verification and browser evidence

- Commands: `frontend/npm run build`, focused Vitest, `backend/mvn test`.
- Browser: existing Playwright/capture workflow at 375px, 768px, 1280px; verify profile popover and common-code return state.
- Work: record fresh outputs and screenshots under `docs/result/20260923/profile-department-level-page-session/`.

### Step 9: Review

- Review scope: specification compliance and code quality separately.
- Work: inspect diff, public API compatibility, tenant/LEVEL scoping, invalid session state behavior, and no unrelated file churn.
- Validation: resolve Critical/Important findings before completion; record Minor findings in progress ledger.
