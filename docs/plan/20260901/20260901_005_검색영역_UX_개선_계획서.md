# 검색 영역 UX 및 검색 기능 개선 계획서

- 작성일: 2026-09-01
- 작성자: GitHub Copilot
- 작업 대상: `MenuManagementPage.tsx`, `PageSearchArea.tsx`

## 구현 계획

### 1단계: state 및 이벤트 핸들러 추가

- `MenuManagementPage.tsx`에 `searchQuery` 상태 추가
- `TextField` `value`, `onChange`, `onKeyDown` 이벤트 연결
- `Enter` 키 감지 시 `requestRefresh(selectedModuleId)` 실행
- Clear 버튼 (`IconButton` + `CloseIcon`) 추가 및 클릭 시 `searchQuery` 빈 문자열 설정

### 2단계: 레이아웃 및 디자인 정리

- `PageSearchArea.tsx`의 컴팩트 패딩 유지 및 아이콘/라벨 시각적 구성
- 레이아웃 순서: `PageHeader` → `PageSearchArea` → `PageMessageArea` → `MenuManagementPanel`

### 3단계: 검증

- `npm run build`
- `npm run test -- tests/menu-management-f1-grid.test.tsx`
